{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "helpers:pinGitHubActionDigests",
    "docker:pinDigests",
    ":maintainLockFilesWeekly"
  ],
  "dependencyDashboard": true,
  "prConcurrentLimit": 5,
  "prHourlyLimit": 2,
  "ignorePaths": [
    "bench/rarpar-bench/config/toolchains.json",
    "bench/rarpar-bench/docker/**"
  ],
  "packageRules": [
    {
      "matchManagers": ["cargo"],
      "groupName": "Rust dependencies"
    },
    {
      "matchManagers": ["github-actions"],
      "groupName": "GitHub Actions",
      "separateMajorMinor": true
    },
    {
      "description": "The generator toolchain is frozen: the RARLAB writers, the FFmpeg encoder image and the par2cmdline-turbo generator in config/toolchains.json, and the Docker bases the toolchain images build from, are pinned deliberately. Changing any of them is an intentional corpus revision (docs/test-corpus.md), never a dependency update.",
      "matchFileNames": [
        "bench/rarpar-bench/config/toolchains.json",
        "bench/rarpar-bench/docker/**"
      ],
      "enabled": false
    }
  ]
}
